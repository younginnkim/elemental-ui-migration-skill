{
  "name": "elemental-ui-migration-mcp",
  "version": "1.0.0",
  "description": "MCP server for migrating Flutter Plover widgets to Elemental UI (elutter)",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc && cp src/mapping.json build/ && chmod 755 build/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
